import types from "./types";

const INITIAL_STATE = {
 
};

const evaluatorReducer = (state = INITIAL_STATE, action) => {
    switch (action.type) {

        default:
            return state;
    }
}

export default evaluatorReducer